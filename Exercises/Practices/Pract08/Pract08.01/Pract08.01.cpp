#include <iostream>

using namespace std;

int main()
{
    int count = 0;

    while (count < 5)
    {
        int n;
        cin >> n;

        if (n > 0)
        {
            count++;
        }
    }
}