#include <iostream>

using namespace std;

const int MATRIX_SIZE = 6;

void readMatrix(int matrix[][MATRIX_SIZE], int rows, int cols)
{
    for (int i = 0; i < MATRIX_SIZE; i++)
    {
        for (int j = 0; j < MATRIX_SIZE; j++)
        {
            cin >> matrix[i][j];
        }
    }
}

int main()
{
    int arr[MATRIX_SIZE][MATRIX_SIZE];



    for (int i = 0; i < n; i++)
    {
        int k;
        cin >> k;
        arr[i] = k;
    }
}