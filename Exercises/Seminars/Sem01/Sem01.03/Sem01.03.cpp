#include <iostream>

using namespace std;

int main()
{
	char letter;
	cin >> letter;

	cout << (letter == 'A' || letter == 'a' || letter == 'E' || letter == 'e' || letter == 'I' || letter == 'i' || letter == 'O' || letter == 'o' || letter == 'U' || letter == 'u') << endl;

	return 0;
}
